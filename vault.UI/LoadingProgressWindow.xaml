<Window x:Class="vault.UI.LoadingProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Operazione in corso"
        Width="420"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Background="#FF101217">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock"
                   Grid.Row="0"
                   Foreground="#FFE7EAF0"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <ProgressBar x:Name="OperationProgressBar"
                     Grid.Row="1"
                     Height="16"
                     Minimum="0"
                     Maximum="100"
                     Value="0"
                     IsIndeterminate="True"
                     Foreground="#FF3B4F72"
                     Background="#FF1B2331"/>

        <TextBlock x:Name="PercentTextBlock"
                   Grid.Row="2"
                   HorizontalAlignment="Right"
                   Foreground="#FFB6C0D4"
                   FontSize="12"
                   Margin="0,8,0,0"
                   Text="In corso..."/>
    </Grid>
</Window>
